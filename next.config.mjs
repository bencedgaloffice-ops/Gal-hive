/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // three / drei ship modern ESM that Next transpiles fine; nothing custom needed.
};

export default nextConfig;
